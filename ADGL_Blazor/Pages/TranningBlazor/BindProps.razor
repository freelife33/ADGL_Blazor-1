@page "/bindprops"

@*<PageTitle>Index</PageTitle>

    <h1>Hello, world!</h1>

    Welcome to your new app.

    <SurveyPrompt Title="How is Blazor working for you?" />
*@

Kurs => @course.CourseName
<br />
Fiyat => @course.Price
<br />
<input type="number" @bind-value="@course.Price" @bind-value:event="oninput" />
<br />
<input type="checkbox" @bind-value="@course.IsActive" checked="@(course.IsActive?"checked":null)" /> Is Active
<br />
<span>Bu kurs @(course.IsActive?"Aktif":"Pasif")</span>
<br />

<select @bind="defaultSelectValue">
    @foreach (var drop in drops)
    {
        <option value="@drop.Name">@drop.Name</option>
    }
</select>

<br />
<span> Seçilen kurs adı :@defaultSelectValue </span>

<EditDeleteButtonComponent IsAdmin="true"></EditDeleteButtonComponent>

@code {
    string defaultSelectValue = "Microservis";
    List<DropdownProps> drops = new List<DropdownProps>()
    {
        new DropdownProps{Id=1, Name="E-Ticaret", Value="30"},
         new DropdownProps{Id=2, Name="Microservis", Value="40"},
          new DropdownProps{Id=3, Name="Sosyal Medya", Value="50"},
           new DropdownProps{Id=4, Name="Dependency Incejtion", Value="60"},
    };




    Models.KursTranning course = new Models.KursTranning()
        {
            Id = 1,
            CourseName = "Blazer Kursu",
            Price = 20,
            IsActive = true
        };
}